spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  datasource:
    url: jdbc:h2~/test;MODE=MySQL;
    username: sa
    password:
    driver-class-name: org.h2.Driver
